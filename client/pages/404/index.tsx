import React from 'react';
import Standard from '@layout/Standard';
import { NOT_FOUND } from '@settings';

function NotFound() {
  return <Standard>{NOT_FOUND.heading}</Standard>;
}

export default NotFound;
